# 全局工作偏好

- 用通俗易懂的中文回答。
- 增加功能、改变既有行为或扩大实现范围前，优先从代码、文档和运行证据中补齐上下文；只有缺失信息会实质影响需求、架构、数据或用户可见行为，且无法安全作出合理假设时才提问。除非用户明确要求只讨论、只计划、只 review 或暂不修改，否则信息足够时直接执行。
- 当任务结束、遇到实质分叉或下一步不明显时，主动给出 2 到 3 条可行路径并标出推荐路径；下一步显然时不必机械列举。
- 默认保持改动范围小而可验证，不顺手重构无关代码；除非用户明确要求，否则不提交、不推送、不改变远端可见性。

## 什么时候调哪个 skill

细节都在 skill 里，这里只管路由。**不要在本文件里重述 skill 的内容**——同一件事写两份，迟早分叉。

| 遇到 | 调 |
| --- | --- |
| 需求模糊、高风险改动、要先对齐验收 | `clarify` |
| bug、性能回归、"这怎么坏的" | `diagnose` |
| 要写测试、test-first | `tdd` |
| review、修 review 问题、复审 | `review-fix-loop` |
| 写 / 改 / 整理项目文档，记一条决策 | `canon` |
| Streamlit 功能增删改 | `streamlit-feature-change` |
| 探索代码结构、找调用链、影响面分析 | `codebase-memory` |
| 界面观感升级、去掉通用 AI 味 | `redesign-skill` |
| 定期扫描代码库找"可加深模块"的重构机会 | `improve-codebase-architecture` |
| 设计深模块（小接口、脏活封装在里面） | `codebase-design` |
| 维护项目领域模型（术语、CONTEXT.md） | `domain-modeling` |
| Office 文档读写 | `officecli` |

**ADR 权威在 canon，不在 domain-modeling。** `domain-modeling` 判断"这个决定该不该记 ADR"仍按它自己的三条件（难以撤回 / 不解释会让人困惑 / 真的做了权衡），但判断为"该记"之后，实际写文件、编号、更新 `adr/README.md` 索引这一步转交 `canon`，不要按 `domain-modeling` 的 `ADR-FORMAT.md` 自己直接写 `docs/adr/`。原因：`domain-modeling` 是从上游 vendor 的，改它内部逻辑会在同步更新时反复冲突；`canon` 是自研的且本来就管着 ADR 索引和守卫测试，两边都想当权威时以 canon 为准。

## 一直生效的护栏

这几条**不能只写在 skill 里**——它们恰恰要在对应 skill 没被调用时生效。

- **别拿文档当活干。** 当前切片的目标、边界、验收明确后，就去执行下一项代码、测试或验证动作。除非发现阻塞该动作的事实错误，不得先重写需求或架构文档。**若连续两轮仍只有文档修改，必须报告真实阻塞或立即执行最小可验证切片。**
- **阶段闸门。** 完成重大探针、消耗一档资源、出现新科学门禁、连续两个切片没有增加原始产品能力，或准备进入下一阶段时，必须重新评估继续、并行、降级、换路线或停止。**不得把"技术上的自然下一步"视为已获授权。**
- **完成判据。** 任务完成前必须确认实际实现、相关验证和本轮 review 都已完成。不得以文档、计划或部分 smoke 代替任务完成。

## 子 Agent 使用

- 不为简单任务机械创建子 Agent。只有任务可以拆成边界清晰、低冲突且可独立验证的工作流时，才主动并行委派。
- 优先把代码调查、调用链分析、测试缺口分析、候选方案验证和独立 review 交给子 Agent；主 Agent 负责架构判断、实现边界、结果整合和全局验证。
- 并行修改代码前必须明确文件所有权，避免多个 Agent 同时改同一文件或强耦合模块；无法安全划分时由主 Agent 串行完成。
- 每个子任务必须明确目标、范围、允许的修改、预期输出和停止条件。子 Agent 默认不修改权威文档，只返回结论、证据、验证结果和风险。
- 主 Agent 不得直接把子 Agent 的结论当成完成证据；整合后必须检查实际改动，并运行与整体改动相匹配的验证。

<!-- codebase-memory-mcp:start -->
# Codebase Knowledge Graph（codebase-memory-mcp）

项目启用了 codebase-memory-mcp 时，代码发现优先走图谱工具，而不是 grep / glob / 文件搜索
（一次结构化查询约 500 tokens，等价的 grep 约 80K）。项目尚未建索引先跑 `index_repository`。

**工具选型、工作流和已知的坑见 `codebase-memory` skill，此处不重复。**

只在这三种情况回退文本搜索：字符串字面量 / 错误消息 / 配置值；非代码文件（Dockerfile、脚本、
配置、文档）；图谱不可用、索引失败或结果不足。
<!-- codebase-memory-mcp:end -->
